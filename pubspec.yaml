name: xam_otp_flutter
description: A new Flutter OTP validation project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  flutter_bloc: ^6.0.5
  bloc_test: ^7.0.4
  http: ^0.12.2
  equatable: ^1.2.5
  connectivity: ^0.4.9+3

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.0.0
flutter:

  uses-material-design: true

